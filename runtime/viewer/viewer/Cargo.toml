[package]
name = "viewer"
description = "A native window for a dualis run: rotate, zoom, scrub."
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
viewer-core = { workspace = true }
pollster = "0.4"
wgpu = "22"
winit = "0.30"
