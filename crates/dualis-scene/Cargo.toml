[package]
name = "dualis-scene"
description = "Where simulated things are, and what a run of them looks like: placement, capture, and the shapes a view can draw"
keywords = ["simulation", "scene", "physics", "visualisation"]
categories = ["science", "simulation"]
readme = "../../README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
dualis-core = { workspace = true }
dualis-units = { workspace = true }
glam = { workspace = true }
