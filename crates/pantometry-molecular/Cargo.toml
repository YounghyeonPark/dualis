[package]
name = "pantometry-molecular"
description = "Molecular dynamics as a simulation domain: Lennard-Jones fluids in periodic boxes, with the thermodynamics that follows"
keywords = ["molecular-dynamics", "lennard-jones", "thermostat", "physics", "simulation"]
categories = ["science", "simulation"]
readme = "../../README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
pantometry-core = { workspace = true }
pantometry-units = { workspace = true }
glam = { workspace = true }
